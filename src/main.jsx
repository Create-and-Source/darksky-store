import React from 'react';
import ReactDOM from 'react-dom/client';
import DarkSkyStore from './DarkSkyStore';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DarkSkyStore />
  </React.StrictMode>
);
